# Andhadhun-server

This is the server config files for the Andhadhun app.   
To run, simply cd into the directory and run `node server.js`. Once that is done, make the POST requests from the Andhadhun app on port 5000.  
The USB port is configured to be COM5 currently.
